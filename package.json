{
  "name": "brevo-checker",
  "version": "1.0.0",
  "description": "Brevo quota checker and GitHub Action",
  "main": "src/action.js",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node src/checker.js",
    "build": "ncc build src/action.js --license licenses.txt -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  },
  "engines": {
    "node": ">=24"
  }
}
